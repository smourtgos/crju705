# Answer keys, exams, private notes — must never enter git history
_private/

# Quarto build artifacts
_site/
/.quarto/

# R artifacts
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Raw data downloads for R/build-anchor-dataset.R (not committed)
data-raw/

# OS
.DS_Store

**/*.quarto_ipynb

# Stray duplicate render artifacts: rendered HTML dropped next to source .qmd
# (e.g. "demos/demo-07-workshop 2.html"). Scoped to .html on purpose -- the
# similarly-named PNGs under _freeze/ are legitimate and are tracked.
* [0-9].html
